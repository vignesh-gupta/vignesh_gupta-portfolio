import { Plus_Jakarta_Sans, Poppins, Preahvihear } from 'next/font/google';

// Used Fonts
export const poppins = Poppins({ subsets: ['latin'], weight: ['600'] });
export const plusJakartaSans = Plus_Jakarta_Sans({
  subsets: ['latin'],
  weight: ['700'],
});
export const preahvihear = Preahvihear({ subsets: ['latin'], weight: ['400'] });
